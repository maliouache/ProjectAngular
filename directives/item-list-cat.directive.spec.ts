import { ItemListCatDirective } from './item-list-cat.directive';

describe('ItemListCatDirective', () => {
  it('should create an instance', () => {
    const directive = new ItemListCatDirective();
    expect(directive).toBeTruthy();
  });
});
