import { ItemCatDirective } from './item-cat.directive';

describe('ItemCatDirective', () => {
  it('should create an instance', () => {
    const directive = new ItemCatDirective();
    expect(directive).toBeTruthy();
  });
});
